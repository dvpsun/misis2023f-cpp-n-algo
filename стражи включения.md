страж включения нужен для избегания повторной компиляции файла.
Если он используется в нескольких других файлах, он будет скомпилирован несколько раз. 
Проверяется до этапа сборки препроцессором

Пример:
```cpp
#ifndef LIBNAME
#define LIBNAME

...

#endif // LIBNAME
```